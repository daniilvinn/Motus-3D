#pragma once

#include <Core/Macros.h>

namespace Motus3D {

	enum class ResourceType : uint8_t {
		MESH,
		TEXTURE,
		MATERIAL,
		HDR,
		AUDIO
	};

	/*
	* @brief Provides full control of resources, including loading / unloading ones, support resouces existing in memory and / or link them between each other.
	*/
	

}
